{
  "name": "hardhat-project",
  "scripts": {
    "deploy": "npx hardhat ignition deploy ./ignition/modules/ExpenseTracker.js --network localhost",
    "localchain": "npx hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.2"
  }
}
